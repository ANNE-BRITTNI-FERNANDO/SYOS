# Database Configuration for XAMPP MySQL
# SYOS Database Connection Settings

# Database Connection Details
db.url=jdbc:mysql://localhost:3306/syos_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.username=syos_user
db.password=temp1234
db.driver=com.mysql.cj.jdbc.Driver

# Connection Pool Settings
db.pool.initial.size=5
db.pool.max.size=20
db.pool.min.idle=5

# Connection Timeout Settings (in milliseconds)
db.connection.timeout=30000
db.idle.timeout=600000
db.max.lifetime=1800000

# Additional MySQL Settings
db.validation.query=SELECT 1
db.test.on.borrow=true
db.test.while.idle=true
db.validation.interval=30000
